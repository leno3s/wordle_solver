{
  "name": "wordle_solver",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild-node-tsc",
    "install": "npm run build",
    "prestart": "if [ ! -e dist/index.js ]; then npm run build; fi",
    "start": "node dist/index.js"
  },
  "author": "leno3s",
  "dependencies": {
    "tsc": "^2.0.4",
    "typescript": "^4.5.5",
    "esbuild": "^0.14.27",
    "esbuild-node-tsc": "^1.8.5"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "prettier": "^2.5.1"
  }
}
